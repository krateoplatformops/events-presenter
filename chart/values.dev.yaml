replicas: 1

config:
  DEBUG: "true"

  DB_HOST: postgres.eventstack-system.svc.cluster.local
  DB_PORT: "5432"
  DB_USER: test
  DB_PASS: test
  DB_NAME: testdb
  DB_PARAMS: "sslmode=disable"
  DB_READY_TIMEOUT: "4m"

service:
  port: 8084

secret:
  create: false
  name: eventrouter-db

ingress:
  enabled: false

resources:
  requests:
    cpu: 10m
    memory: 32Mi
  limits:
    cpu: 100m
    memory: 128Mi
