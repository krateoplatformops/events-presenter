apiVersion: v1
kind: Service
metadata:
  name: {{ include "events-presenter.fullname" . }}
  labels:
    {{- include "events-presenter.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "events-presenter.selectorLabels" . | nindent 4 }}
